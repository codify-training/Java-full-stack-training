OOP Concepts: Object Oriented Programming.
 -> Oop is a way of writing programs by using Objects(real world things) instead of just functions and logics.
 -> Oop Helps in breaking large application into smaller objects making systems easier to maintain.
 -> It allows Protects data(Encapsulation), Code Reuse(Inheritence), Supports flexibilty, hides internal complexity.


 Lets take banking app.
          Credit system, Salary Processing, Bank Employees, customers, Loans  

  C program file
   credit
   salary
   BankEmploee
   Customer
    - money credit
    - Bal enq
    - money Dep.
   Loans
     Give
     Take load


Credit
  - data, logic
Salary
  - data, logic
BankEmploee
   - data -logic.
Customer
   - data -logic



 Protects data using Encapsulation:
 ----------------------------------
 Encapsulation means wrapping of data(variables) and its method(functions) together into a single unit called class and restrict direct access to data.

 class BankAccount{
    private double balance = 0;

    public void deposit(int amt){
        if(amt > 40000){
            System.out.println("verify aadhar and take amount");
        }
        balance = balance + amt;
    }
   public void withdrawl(int amt){
        balance = balance - amt;
    }
   public double getBalance() {
       return balance;
   }
 }

Here balance is private so that no one from outside modify it directly, instead it is modified via methods.

Why encapsulation was designed.
  -> Protect data.
  -> Avoid unauthorized modifiction.


Code Reuse - Using Inheritence.
------------------------------------
  Grandfather 
    10kg gold 
    1 maruthi car
    Snoring()
  Father 
    10kg gold 
    1 maruthi car
    2 houses 
    Snoring()
  Son
    10kg gold 
    1 maruthi car
    2 houses 
    1 company
    Snoring()

-> It allows one class to acquire the (properties/data/varibles) and (behaviour/function/methods) of another class.    
    The class that acquires properties and bhaviour is child class, 
    The class that provides properties and bhaviour to child class is Parent class, 

    Why it is designed:
      -> To increase code Reusability
      -> Reduce code Dupilication.

      EX: Program
      Java-full-stack-training\upskillhouseprograms\src\main\java\org\example\oops\inheritance\InheritanceDemo.java


Supports flexibilty using Polymorphism.
-----------------------------------------
Its greek word, poly(many) morphs(forms).
 The ability of same function or behaviuor to be present in multiple forms is called polymorphism. 
 EX: upskillhouseprograms\src\main\java\org\example\oops\polymorphism\PolymorhDemo.java

Two types
  Compile Time polymorphism(Method Overloading) - Same behaviour different forms.
     These Methods will reside in same class.
     The arguments will be different.
     The method name and return type will be same.

     The method resolution(which method to be picked among differnt methods of same behaviour) will happen during compiletime itself.

    EX: 
    public String joinInfo(String info1, String info2, String info3){
        return info1 + info2 + info3;
    }

    public String joinInfo(String info1, String info2){
        return info1 + info2;
    }
      . 
  Runtime Polymorphism(Method Overriding).
  ---------------------------------------- 
     These Methods will reside in different class.
     The arguments will be same.
     The method name and return type will be same.

     The method resolution(which method to be picked among differnt methods of same behaviour) will happen during Runtime.

     D:\Tutions\upskillhouse\Java-full-stack-training\upskillhouseprograms\src\main\java\org\example\oops\polymorphism\RuntimePolymorphism.java


Abstraction:
------------ 
It is process of hiding complex implementation details of a system and showing only the essential feastures to the user.

How abstraction achieved in java ?
----------------------------------
we have 2 tools to achieve abstraction.
1. Abstract Class
2. Interfaces.

Abstract Class:
---------------
  -> if we keep abstract keyword infront of class, then it will become abstract class.
  -> It can have abstract methods(method without bodies) and normal methods(methods with bodies).
  -> If a class contains an abstract method then that class becomes abstract class.
  -> You cannot object for an abstract class.(the reason is this is not a complete class.)

  abstract Methods:
    - If we keep abstract keyword infront of a method then it will become abstract method.
    - It will only definition. It wont have implementation.
    

Interfaces:
----------
 It defines a contract that an implementing class must follow. they specify what to do but not how to do.

An interface is a collection of abstract methods, i.e it will have only declaration not definition.

  -> interface has 100% abstraction
  -> Cannot create object directly.
  -> Supports multiple inheritence. - We can discuss tomorrow.

